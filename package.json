{
  "name": "jread",
  "version": "0.8.148",
  "private": true,
  "description": "JRead — Chrome Extension 純閱讀模式。開發端 package.json，僅用於測試。",
  "scripts": {
    "test": "mocha \"test/**/*.spec.js\" --ignore \"test/e2e/**\" --timeout 30000",
    "test:e2e": "mocha \"test/e2e/**/*.spec.js\" --timeout 60000",
    "debug": "node tools/debug-harness.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "mocha": "^10.2.0",
    "playwright": "^1.59.1"
  }
}
