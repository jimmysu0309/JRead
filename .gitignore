node_modules/

# 內部文件（QA 驗收規格等），不上傳
docs/excluded/
.backups/
.claude/settings.local.json
.DS_Store

# 自動化除錯 harness 輸出（screenshot 等）
.playwright-mcp/
# Playwright 執行追蹤產物（page-rounds / debug harness 跑完留下）
test-results/
# debug-harness 載 unpacked extension 用的 persistent profile（每次跑重建）
.debug-profile/
# Page Rounds 視覺驗收截圖（在 docs/excluded/ 下，已被 docs/excluded/ 規則涵蓋）
# 舊 Playwright MCP 設定檔（目前已改用 tools/debug-harness.js 不再需要）
.playwright-mcp.json
# Page Rounds 批次跑站腳本——只讀本地 gitignored 的 docs/excluded/page-rounds-sites.md，
# 純內部驗收工具，不上傳（2026-06-18 移出版控、保留本地）
tools/batch-page-rounds.sh

# Claude Design 原始下載（HTML/zip/uploads），已抽取必要 source 至 store-assets/sources/
Claude design/

# Instapaper Full API consumer 金鑰（app 層 secret，不進 public repo / git 歷史）
# 源檔 + ios-build.sh rsync 到 Safari Resources 的鏡像都要擋，否則 commit
# 「iOS build artifacts — Resources mirror」時會透過鏡像洩漏金鑰。
jread/lib/instapaper-keys.js
safari-app/**/Resources/lib/instapaper-keys.js

# Safari build 中間產物
# iOS build 的 BUILD_DIR 已移到 $TMPDIR（避開 iCloud Drive 同步衝突），不生在 repo 內。
# 規則保留作保險：萬一未來改回本地路徑、或 safari-app/ 殘留歷史 .pkg，不會誤 commit。
# （macOS Developer ID .pkg 軌已於 v0.7.249 退役——Safari 改由單一 iOS binary 涵蓋。）
safari-app/build/
safari-app/*.pkg

# Xcode user-specific state
*.xcuserdata/
xcuserdata/

# Claude Code 本機協作規則與 session 狀態（含個人資訊 / 專案流程），不進公開 repo
# 比照姊妹專案 Shinkansen：CLAUDE.md 與整個 .claude/（含 skills）僅存本機
CLAUDE.md
CLAUDE-TEMPLATE-*.md
.claude/

# Codex 本機協作規則與設定（AGENTS.md 為 CLAUDE.md 的 Codex 版），比照不進公開 repo
# （Jimmy 2026-07-30 裁定）
AGENTS.md
.agents/
.codex/

# 回歸測試套件（Jimmy 2026-08-06 裁定移出公開 repo）
# 動機：393 支 spec + 309 個 fixture 記錄了大量開發過程與判斷脈絡，不公開。
# 現住私有 repo github.com/jimmysu0309/JRead-test，clone 回本路徑掛載——
# 路徑不變是刻意的：spec 內 path.join(__dirname, '..', ...) 讀 SPEC.md /
# jread/ / safari-app/ 等相對路徑因此完全不用改，npm test 與 release.sh 照跑。
# 不用 submodule：那會在公開 repo 留下 .gitmodules 指向私有 URL。
# 注意 fixture 內含自真實站點擷取的第三方 HTML，本就不宜公開散布。
# 重新掛載：git clone git@github.com:jimmysu0309/JRead-test.git test
test/

# 內部文件完整版（Jimmy 2026-08-06 裁定不公開）
# SPEC.md / CHANGELOG.md / docs/CHANGELOG-archive.md 逐版記錄了每個 bug 的根因、
# 試錯幾輪、哪些方向撤回、誰哪天回報什麼——是「開發過程」的最大載體。
# 公開端的產品說明改由 README.md / README.en.md + docs/index.html 承擔（本來就
# 寫得完整），不另做瘦身版：同一份事實兩份實作必然 drift（工作流原則 5）。
#
# 檔案仍在原位（forcing function 與日常工作都讀根目錄），版控走「疊加的第二個
# 倉庫」：.private-git 是它的 .git、work-tree 指向本根目錄，推到私有 repo
# github.com/jimmysu0309/JRead-docs。2026-08-07 起追蹤範圍擴到 8 檔——除了下面
# 三份逐版文件，還有協作規則本體 CLAUDE.md / AGENTS.md / .claude/（那幾份也在
# 本檔的忽略清單內，兩邊都不收就等於只活在本機）。操作一律加 --git-dir：
#   git --git-dir=.private-git status --short
#   git --git-dir=.private-git add -u && git --git-dir=.private-git commit -m "…"
#   git --git-dir=.private-git push
# 新環境怎麼把兩個私有 repo 接回來：見 docs/DEV-SETUP.md（單一資料源，刻意留在
# 公開端——接法本身若只寫在私有文件裡，新環境永遠讀不到）
SPEC.md
CHANGELOG.md
docs/CHANGELOG-archive.md
.private-git/

# release 產物本機收納（Jimmy 2026-08-20 規則）：每次 release 後把 GitHub Release
# 的三個 zip（Chrome / Firefox sideload / AMO source）下載進 dist/、刪掉舊版本。
# 不進版控——那三個 zip 是 Actions 的建置產物、GitHub Releases 上已有權威一份，
# 收進公開 repo 只會每版灌 15M 二進位進歷史（且刪不掉）。流程見 /release skill 第 6 節。
dist/
