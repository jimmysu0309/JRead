node_modules/
.backups/
.claude/settings.local.json
.DS_Store

# 自動化除錯 harness 輸出（screenshot 等）
.playwright-mcp/
# 舊 Playwright MCP 設定檔（目前已改用 tools/debug-harness.js 不再需要）
.playwright-mcp.json

# Claude Design 原始下載（HTML/zip/uploads），已抽取必要 source 至 store-assets/sources/
Claude design/
